<UserControl
    x:Class="Hexblick.Presentations.FontDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:Hexblick.Presentations"
    mc:Ignorable="d">

    <Grid>
      <ComboBox ItemsSource="{x:Bind Fonts}">
        <ComboBox.ItemTemplate>
          <DataTemplate x:DataType="local:FontInfo">
            <TextBlock Text="{x:Bind FamilyName}" FontFamily="{x:Bind FamilyName}" FontSize="20"/>
          </DataTemplate>
        </ComboBox.ItemTemplate>
      </ComboBox>
    </Grid>
</UserControl>
