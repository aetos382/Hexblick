<Window
  x:Class="Hexblick.Presentations.MainWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:i="using:Microsoft.Xaml.Interactivity"
  xmlns:hp="using:Hexblick.Presentations"
  xmlns:hb="using:Hexblick.Behaviors"
  mc:Ignorable="d"
  Title="Hexblick">

  <Window.SystemBackdrop>
    <MicaBackdrop />
  </Window.SystemBackdrop>

  <Page>
    <i:Interaction.Behaviors>
      <hb:MultipleFileOpenPickerBehavior MessageHandler="{x:Bind ViewModel.InteractionMessenger.MultipleFileOpenPickerRequestHandler}"/>
      <hb:ConfirmSaveBehavior MessageHandler="{x:Bind ViewModel.InteractionMessenger.ConfirmSaveRequestHandler}"/>
    </i:Interaction.Behaviors>

    <StackPanel>

      <MenuBar>
        <MenuBarItem x:Uid="MenuFile" AccessKey="F">
          <MenuFlyoutItem x:Uid="MenuFileNew" AccessKey="N" Command="{x:Bind ViewModel.NewDocumentCommand}"/>
          <MenuFlyoutItem x:Uid="MenuFileOpen" Command="{x:Bind OpenFileCommand}"/>
          <MenuFlyoutSeparator/>
          <MenuFlyoutItem x:Uid="MenuFileSave" AccessKey="S" Command="{x:Bind ViewModel.SaveFileCommand}" CommandParameter="{x:Bind ViewModel.ActiveDocument.Value, Mode=OneWay}">
            <MenuFlyoutItem.KeyboardAccelerators>
              <KeyboardAccelerator Key="S" Modifiers="Control"/>
            </MenuFlyoutItem.KeyboardAccelerators>
          </MenuFlyoutItem>
          <MenuFlyoutItem Text="Save As"/>
          <MenuFlyoutItem Text="Save All"/>
          <MenuFlyoutSeparator/>
          <MenuFlyoutItem Text="Recently used files"/>
          <MenuFlyoutSeparator/>
          <MenuFlyoutItem Text="Close"/>
          <MenuFlyoutItem Text="Close All"/>
          <MenuFlyoutItem Text="Close Unmodified"/>
          <MenuFlyoutSeparator/>
          <MenuFlyoutItem Text="Exit" Command="{x:Bind ExitCommand}">
            <MenuFlyoutItem.KeyboardAccelerators>
              <KeyboardAccelerator Key="F4" Modifiers="Menu"/>
            </MenuFlyoutItem.KeyboardAccelerators>
          </MenuFlyoutItem>
        </MenuBarItem>
        <MenuBarItem x:Uid="MenuWindow" AccessKey="W">
          <MenuFlyoutItem x:Uid="MenuWindowNew" Command="{x:Bind ViewModel.NewWindowCommand}"/>
        </MenuBarItem>
      </MenuBar>

      <TabView
        x:Name="TabView"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch"
        IsAddTabButtonVisible="False"
        TabItemsSource="{x:Bind ViewModel.EditorViewModels}">

        <TabView.TabItemTemplate>
          <DataTemplate x:DataType="hp:EditorControlViewModel">
            <TabViewItem IsClosable="true">

              <i:Interaction.Behaviors>
                <hb:ConfirmSaveBehavior MessageHandler="{x:Bind InteractionMessenger.ConfirmSaveRequestHandler}"/>
              </i:Interaction.Behaviors>

              <TabViewItem.IconSource>
                <ImageIconSource ImageSource="{x:Bind Icon.Value}"/>
              </TabViewItem.IconSource>
              <TabViewItem.HeaderTemplate>
                <DataTemplate x:DataType="hp:EditorControlViewModel">
                  <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{x:Bind Title.Value, Mode=OneWay}"/>
                    <TextBlock Text=" *" Visibility="{x:Bind IsDirty.Value, Mode=OneWay}"/>
                  </StackPanel>
                </DataTemplate>
              </TabViewItem.HeaderTemplate>
              <TabViewItem.Content>
                <hp:EditorControl ViewModel="{x:Bind}"/>
              </TabViewItem.Content>
            </TabViewItem>
          </DataTemplate>
        </TabView.TabItemTemplate>

      </TabView>

    </StackPanel>
  </Page>

</Window>
