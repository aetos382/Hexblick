---
paths:
  - "**/*.cs"
---

# C# ソースコードに関するルール

- `using` ディレクティブは、`.` で区切った第一セグメントごとにグループ化する。
- グループの並び順は以下のとおりとする。
  1. System
  2. Microsoft
  3. Windows
  4. WinRT
  5. NuGet で参照しているパッケージの名前空間
    - アルファベット順で並べる。
  6. プロジェクト内の名前空間
- 同じグループ内ではアルファベット順で並べる。
